ФИНАЛЬНЫЙ ПРОМТ: ДОВОДКА ДО ПРОДАКШЕНА.
Цель — ТЗ без единой ошибки, готовое к публикации в ЕИС.

╔══════════════════════════════════════════════════════╗
║  УРОВЕНЬ 1 — АВТОМАТИЧЕСКИЕ ТЕСТЫ                    ║
║  Запускать при каждой генерации, блокировать экспорт ║
╚══════════════════════════════════════════════════════╝

Реализовать функцию validate_tz(document) → ValidationResult
Результат содержит: errors[], warnings[], passed: bool

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-01: МЕТА-КОММЕНТАРИИ В ТЕКСТЕ (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Паттерны для поиска по всему документу:
  r'\[!?\]'                    → "[!]"
  r'\(!\)'                     → "(!)"
  r'Требуется .{0,50}значени'  → "[!] Требуется..."
  r'TODO|FIXME|XXX|HACK'       → технические метки
  r'\*\*\s*Примечание'         → маркированные примечания
  r'<!--.*?-->'                → HTML-комментарии
  r'\[Требуется'               → "[Требуется уточнить]"

Если найдено хоть одно → ОШИБКА, экспорт заблокирован.
Автоисправление: удалить все вхождения, повторить генерацию
только для строк где было вхождение.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-02: ЗАПРЕЩЁННЫЕ ФОРМУЛИРОВКИ 44-ФЗ (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Статья 33 44-ФЗ запрещает:

ПАТТЕРНЫ → ДЕЙСТВИЕ:
r'по требованиям [Зз]аказчика'      → ОШИБКА + удалить строку
r'на усмотрение [ИиПп]'             → ОШИБКА + удалить строку
r'уточняется при поставке'           → ОШИБКА + удалить строку
r'согласно тех\w+ документ'          → ОШИБКА + удалить строку
r'не хуже аналогов'                  → ОШИБКА + удалить строку
r'определяется [ПпПроизвод]\w+'     → ОШИБКА + удалить строку
r'по согласованию с [Зз]аказчиком'  → ОШИБКА если в характ.
r'в ассортименте'                    → ПРЕДУПРЕЖДЕНИЕ

Автоисправление: для каждой удалённой строки запросить
у LLM корректную измеримую формулировку и вставить.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-03: ТОВАРНЫЕ ЗНАКИ БЕЗ "ИЛИ ЭКВИВАЛЕНТ" (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Словарь брендов для проверки:
БРЕНДЫ = [
  'Intel', 'AMD', 'NVIDIA', 'Qualcomm',
  'Samsung', 'Seagate', 'WD', 'Western Digital', 'Crucial',
  'Kingston', 'Corsair', 'G.Skill',
  'ASUS', 'Gigabyte', 'MSI', 'ASRock',
  'HP', 'Dell', 'Lenovo', 'Acer', 'ASUS', 'Apple',
  'Cisco', 'TP-Link', 'D-Link', 'Keenetic',
  'Astra', 'FreeSync', 'G-SYNC', 'Thunderbolt',
  'Windows', 'Microsoft', 'Adobe', 'AutoCAD', 'Autodesk',
  'Kaspersky', 'Dr.Web', 'ESET',
  # добавить все бренды с которыми работает система
]

Для каждого бренда в тексте:
  if brand in text and 'или эквивалент' not in context(brand, 50):
    → ОШИБКА: "Бренд {brand} без 'или эквивалент'"
    → Автоисправление: добавить "или эквивалент" после упоминания

Исключения (не требуют эквивалента — это стандарты):
СТАНДАРТЫ = ['USB', 'HDMI', 'DisplayPort', 'DDR4', 'DDR5',
             'NVMe', 'SATA', 'PCIe', 'RJ-45', 'RJ-11',
             'IEEE', 'Wi-Fi', 'Bluetooth', 'VESA',
             'x86', 'x86-64', 'ARM', 'RISC-V',
             'IPS', 'VA', 'TN', 'OLED',
             'PDF', 'DOCX', 'XLSX', 'ODF', 'XML',
             'TCP/IP', 'HTTP', 'HTTPS', 'LDAP', 'AD',
             'ГОСт', 'ГОСТ', 'ТР ТС', 'ISO', 'IEC']

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-04: НЕИЗМЕРИМЫЕ ХАРАКТЕРИСТИКИ (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Для каждой строки таблицы характеристик:

value = ячейка "Значение характеристики"

# Допустимые паттерны:
VALID_PATTERNS = [
  r'не менее \d',      # не менее 8 ГБ
  r'не более \d',      # не более 65 Вт
  r'не ниже',          # не ниже «А»
  r'не выше',
  r'от \d.+до \d',     # от 5 до 35
  r'\d',               # любое число
  r'в комплекте',
  r'наличие',
  r'отсутствие',
  r'соответствие',
  r'обязательн',
  r'поддерживается',
  r'предусмотрен',
  r'или эквивалент',
  r'или аналог',
  r'тип$',             # просто "тип" в единицах измерения
  r'^—$',              # прочерк в единицах измерения
  r'x86-64',
  r'DDR\d',
  r'NVMe|SATA|PCIe',
  r'IPS|VA|TN',
  r'RJ-\d+',
  r'USB \d',
  r'HDMI|DisplayPort',
  r'Astra|ALT|РЕД ОС',
  r'Windows|Linux',
]

if not any(re.search(p, value, re.IGNORECASE) for p in VALID_PATTERNS):
  → ОШИБКА: "Неизмеримое требование: {param_name} = {value}"
  → Автоисправление: повторная генерация только этой строки

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-05: ДУБЛИРУЮЩИЕСЯ ХАРАКТЕРИСТИКИ (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Для каждого приложения:
  seen_names = {}
  for row in table.rows:
    name_key = normalize(row.cells[0].text)
    # normalize = lower + убрать пунктуацию + убрать пробелы
    if name_key in seen_names:
      → ОШИБКА: "Дубль: {param_name} (строки {i} и {j})"
      → Автоисправление: удалить строку с меньшим количеством
        информации в значении

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-06: КОНФЛИКТУЮЩИЕ ТРЕБОВАНИЯ (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Проверить логические конфликты:

a) Потребление > Мощность БП:
   if consumption_w > power_supply_w:
     → ОШИБКА: "Потребление {consumption}Вт > БП {psu}Вт"

b) TDP процессора > Мощность всего ПК:
   if cpu_tdp > total_consumption:
     → ОШИБКА

c) Частота памяти не поддерживается стандартом:
   if ddr_type == 'DDR4' and frequency > 6400:
     → ПРЕДУПРЕЖДЕНИЕ

d) Диагональ монитора vs разрешение:
   if diagonal >= 27 and resolution == 'FHD':
     → ПРЕДУПРЕЖДЕНИЕ: "27" с FHD — низкая плотность пикселей"
   if diagonal <= 22 and resolution == 'QHD':
     → ПРЕДУПРЕЖДЕНИЕ: "QHD на экране до 22" — избыточно"

e) Время отклика GtG vs тип матрицы:
   if matrix == 'IPS' and response_time < 1:
     → ПРЕДУПРЕЖДЕНИЕ: "IPS < 1 мс — нереалистично"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-07: КОЛИЧЕСТВА (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Для каждой позиции:
  qty_section1 = из сводной таблицы раздела 1
  qty_appendix = из заголовка Приложения N
  if qty_section1 != qty_appendix:
    → ОШИБКА: "Кол-во {item}: в разделе 1 = {a}, в Приложении = {b}"

Проверить что quantity > 0 для всех позиций.
Проверить что единицы измерения совпадают (шт./шт., л./л.)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-08: НОРМАТИВНАЯ БАЗА (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
УСТАРЕВШИЕ — найти и заменить:
  "ПП РФ.*?№ 878"  → заменить на ПП №1875 от 23.12.2024
  "ПП РФ.*?№ 616"  → заменить на ПП №1875 от 23.12.2024
  "ПП РФ.*?№ 925"  → заменить на ПП №1875 от 23.12.2024
  "от 30.04.2020"  → заменить (дата ПП №616)
  "от 26.09.2016"  → заменить (дата ПП №878)

ОБЯЗАТЕЛЬНЫЕ ДЛЯ ТОВАРОВ — проверить наличие:
  "44-ФЗ"               → если нет → ОШИБКА
  "1875"                → если нет → ОШИБКА
  "719"                 → если нет → ОШИБКА
  "ТР ТС 004"           → если нет → ПРЕДУПРЕЖДЕНИЕ
  "ТР ТС 020"           → если нет → ПРЕДУПРЕЖДЕНИЕ

ЗАПРЕЩЁННЫЕ ДЛЯ УСЛУГ — проверить отсутствие:
  if category == 'УСЛУГА':
    if "1875" in normative_section:
      → ОШИБКА: "ПП №1875 не применяется к услугам"
    if "719" in normative_section:
      → ОШИБКА: "ПП №719 не применяется к услугам"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-09: СТРУКТУРА ДОКУМЕНТА (блокирующий)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Обязательные разделы для ТОВАРОВ:
  □ Раздел 1: Наименование, Заказчик, Исполнитель, сроки
  □ Раздел 2: Требования к предмету закупки
  □ Раздел 3: Пуско-наладочные работы
  □ Раздел 4: Гарантия качества
  □ Раздел 5: Тара и упаковка
  □ Раздел 6: Место, сроки и условия поставки
  □ Раздел 7: Перечень нормативных актов
  □ Приложения: N штук == количеству позиций

Если отсутствует хоть один раздел → ОШИБКА

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-10: КЛАСС ЭНЕРГОЭФФЕКТИВНОСТИ (предупреждение)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Для товаров попадающих под требования энергоэффективности
(мониторы, ПК, ИБП, принтеры):

  Значение должно быть: 'не ниже «А»' или 'не ниже A'
  
  Допустимые паттерны:
  r'не ниже.{0,5}[«"]?[AА][»"]?'   → OK
  r'класс.{0,20}[AА]'               → OK
  
  Если значение = 'не ниже «А» [!]...' → уже поймает TEST-01
  Если значение = '' или 'по требованиям' → поймает TEST-04

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-11: ПРОВЕРКА НА ОГРАНИЧЕНИЕ КОНКУРЕНЦИИ (ФАС)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Анализировать совокупность характеристик:

a) Уникальные комбинации — под одного производителя:
   Проверить: если 3+ нестандартных параметра
   совпадают с конкретной моделью → ПРЕДУПРЕЖДЕНИЕ
   "Возможно ограничение конкуренции (проверьте ФАС)"

b) Завышенные требования:
   RAM > 64 ГБ для "офисного ПК" → ПРЕДУПРЕЖДЕНИЕ
   SSD > 2 ТБ для "офисного ПК" → ПРЕДУПРЕЖДЕНИЕ
   Монитор > 32" для "рабочего места" → ПРЕДУПРЕЖДЕНИЕ

c) Специфические интерфейсы:
   Thunderbolt без "или эквивалент" → ОШИБКА TEST-03
   Lightning → ОШИБКА TEST-03

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST-12: ЧИТАЕМОСТЬ И ФОРМАТ DOCX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
После сохранения DOCX:

a) Открыть файл повторно через python-docx:
   doc2 = Document(filepath)
   Если ошибка при открытии → ОШИБКА "Повреждённый файл"

b) Проверить что текст читаем (нет кракозябр):
   for para in doc2.paragraphs:
     try:
       text = para.text.encode('utf-8').decode('utf-8')
     except:
       → ОШИБКА "Проблема кодировки в параграфе"

c) Проверить шрифт:
   for run in doc2.paragraphs[0].runs:
     if run.font.name not in ['Times New Roman', 'Arial',
                               'Calibri', None]:
       → ПРЕДУПРЕЖДЕНИЕ "Нестандартный шрифт"

d) Размер файла:
   if file_size < 5_000:  # байт
     → ОШИБКА "Файл подозрительно мал"
   if file_size > 50_000_000:  # 50 МБ
     → ПРЕДУПРЕЖДЕНИЕ "Файл очень большой"

╔══════════════════════════════════════════════════════╗
║  УРОВЕНЬ 2 — ОТОБРАЖЕНИЕ РЕЗУЛЬТАТОВ                 ║
╚══════════════════════════════════════════════════════╝

UI после генерации показывает:

┌─────────────────────────────────────────────────────┐
│  ПРОВЕРКА ТЗ ПЕРЕД ПУБЛИКАЦИЕЙ В ЕИС                │
├─────────────────────────────────────────────────────┤
│  ✅ TEST-01 Мета-комментарии          ПРОЙДЕН       │
│  ✅ TEST-02 Запрещённые формулировки  ПРОЙДЕН       │
│  ✅ TEST-03 Товарные знаки            ПРОЙДЕН       │
│  ✅ TEST-04 Измеримость требований    ПРОЙДЕН       │
│  ✅ TEST-05 Дублирование строк        ПРОЙДЕН       │
│  ✅ TEST-06 Логические конфликты      ПРОЙДЕН       │
│  ✅ TEST-07 Количества               ПРОЙДЕН       │
│  ✅ TEST-08 Нормативная база          ПРОЙДЕН       │
│  ✅ TEST-09 Структура документа       ПРОЙДЕН       │
│  ⚠️  TEST-11 Ограничение конкуренции  ПРЕДУПРЕЖДЕН  │
│        └ RAM 128 ГБ для офисного ПК — проверьте    │
│  ✅ TEST-12 Читаемость DOCX           ПРОЙДЕН       │
├─────────────────────────────────────────────────────┤
│  Итог: 11/12 пройдено, 1 предупреждение            │
│  ТЗ готово к публикации в ЕИС                      │
│                                                     │
│  [Скачать DOCX]    [Скачать PDF]                   │
└─────────────────────────────────────────────────────┘

При ОШИБКАХ (не предупреждениях):
┌─────────────────────────────────────────────────────┐
│  ❌ TEST-01 Мета-комментарии          ОШИБКА        │
│        └ Строка 47, Приложение 2:                  │
│          "не ниже A [!] Требуется..."              │
│  ❌ TEST-07 Количества               ОШИБКА        │
│        └ Монитор: раздел 1 = 1 шт, Прил.2 = 60 шт │
├─────────────────────────────────────────────────────┤
│  Найдено ошибок: 2  │  ТЗ не готово к публикации  │
│                                                     │
│  [Исправить автоматически]  [Показать подробно]    │
│  [Скачать всё равно ⚠️]                             │
└─────────────────────────────────────────────────────┘

Кнопка "Скачать всё равно" работает но
показывает красный баннер: "ВНИМАНИЕ: Найдены ошибки.
ТЗ может быть отклонено ФАС или ЕИС."

╔══════════════════════════════════════════════════════╗
║  УРОВЕНЬ 3 — АВТОИСПРАВЛЕНИЕ                         ║
╚══════════════════════════════════════════════════════╝

Кнопка "Исправить автоматически":

1. Запустить все тесты → получить список ошибок
2. Для каждой ошибки применить fix:

   fix_map = {
     'TEST-01': remove_meta_comments,
     'TEST-02': remove_banned_phrases_and_regenerate,
     'TEST-03': add_or_equivalent,
     'TEST-05': remove_duplicate_rows,
     'TEST-07': sync_quantities_from_source,
     'TEST-08': fix_normative_references,
   }

3. TEST-04 (неизмеримые) — вызвать LLM:
   prompt = f"""
   Характеристика "{param_name}" имеет значение "{value}".
   Это неизмеримое требование. Замени на конкретное
   числовое требование с "не менее" или "не более".
   Категория товара: {category}.
   Верни только новое значение, без пояснений.
   """
   new_value = llm(prompt)
   replace_in_table(param_name, new_value)

4. После всех исправлений → повторить все тесты
5. Если остались ошибки → показать только их
6. Максимум 3 итерации автоисправления

╔══════════════════════════════════════════════════════╗
║  УРОВЕНЬ 4 — СЦЕНАРНЫЕ ТЕСТЫ (запустить один раз   ║
║  после деплоя, результаты сохранить)                ║
╚══════════════════════════════════════════════════════╝

ТЕСТ A — Базовый товар (системный блок):
  Вход: "Системный блок, 1 шт."
  Ожидание:
    □ 12/12 тестов пройдено
    □ Нет [!] в документе
    □ Есть ПП №1875
    □ Нет Wi-Fi/Bluetooth в дефолте
    □ Форм-фактор не зафиксирован как SFF
    □ DOCX открывается без ошибок

ТЕСТ B — Восстановление количеств:
  Вход: DOCX где Системный блок=30, Монитор=60, DVD=5
  Ожидание:
    □ В новом ТЗ: 30 / 60 / 5 (не 1/1/5)
    □ TEST-07 пройден

ТЕСТ C — Услуга:
  Вход: "Техническое обслуживание ПК, 12 месяцев"
  Ожидание:
    □ Нет ПП №1875 в нормативке
    □ Нет раздела про тару и упаковку
    □ Есть раздел SLA
    □ Есть требования к квалификации

ТЕСТ D — ПО:
  Вход: "Антивирус Kaspersky, 50 лицензий, 1 год"
  Ожидание:
    □ "Kaspersky или эквивалент" (не просто Kaspersky)
    □ Есть ПП №1236 в нормативке
    □ Нет ПП №1875 в нормативке
    □ Указано "Наличие в реестре Минцифры: обязательно"
    □ Количество: 50 лицензий сохранено

ТЕСТ E — Стрессовый (многопозиционное ТЗ):
  Вход: 10 позиций разных категорий
  Ожидание:
    □ 10 Приложений в документе
    □ Все количества совпадают
    □ 12/12 тестов пройдено
    □ DOCX открывается, вес < 5 МБ

ТЕСТ F — Проверка ФАС-риска:
  Вход: "Ноутбук MacBook Pro 16, 1 шт."
  Ожидание:
    □ "Apple" → "или эквивалент" добавлен
    □ "MacBook" → заменён на описательные характеристики
    □ TEST-11 предупреждение если характеристики
      слишком специфичны

Все 6 тестов должны пройти.
Если хоть один провален → исправить до прохождения всех.