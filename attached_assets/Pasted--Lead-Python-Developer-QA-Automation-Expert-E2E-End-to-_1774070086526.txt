Ты — Lead Python Developer и QA Automation Expert. Твоя цель: провести глубокий E2E-тест (End-to-End) приложения для генерации Технических заданий (ТЗ), исправить все найденные архитектурные и логические баги и предоставить 100% рабочий, чистый код, готовый к продакшену (соблюдай PEP 8, SOLID). Качество важнее скорости.

Я загрузил исходные шаблоны .docx в корень проекта. Я НЕ буду создавать папки вручную, скрипт должен сделать всё сам.

ЗАДАЧА: Написать автоматизированный тестовый пайплайн, который сам подготовит окружение, сымитирует работу пользователя, сгенерирует документ, а затем проверит его содержимое на соответствие строгим юридическим правилам 44-ФЗ. После этого — отрефакторить основной код приложения.

ЭТАП 1: Авто-настройка окружения и Симуляция (`e2e_test.py`)
Напиши скрипт `e2e_test.py`, который:
1. Автоматически создает папки `templates/` и `test_output/` (используй os.makedirs(..., exist_ok=True)).
2. Находит все файлы `.docx` в корне проекта и программно перемещает/копирует их в папку `templates/` (если их там еще нет).
3. Имитирует ручной ввод пользователя: объект закупки = "ноутбук asus x1503", сроки = 60 дней, количество = 10 шт.
4. Запускает основную логику генерации ТЗ с этими данными и сохраняет итог в `test_output/test_asus_tz.docx`.

ЭТАП 2: Парсинг и Валидация сгенерированного файла
Скрипт должен открыть созданный `test_asus_tz.docx` (через python-docx), извлечь текст таблиц и абзацев, и провести жесткие проверки (asserts):
- Поиск названия: Убедиться, что "asus x1503" успешно интегрировалось в документ.
- Антимонопольный контроль (Негативные тесты): Убедиться, что в тексте НЕТ слов "выписку" и "НДВ", а также фразы "не ранее чем за 12 месяцев". Если они есть — тест падает с понятной ошибкой.
- Юридический контроль (Позитивные тесты): Убедиться, что есть фразы "Цена Договора является твердой", "или эквивалент" и требование про "лицензию ФСТЭК России" в разделе пуско-наладочных работ.

ЭТАП 3: Глубокий рефакторинг основного приложения
Опираясь на то, как сейчас написан код генерации, перепиши его до идеала:
- Проверь на скрытые угрозы: Убедись, что нет неинициализированных переменных.
- Оберни все файловые операции (открытие шаблона, сохранение) в надежные блоки `try-except`. 
- Убедись, что генератор сам заменяет старые юридические формулировки (про выписку и НДВ) на новые, если находит их в исходном шаблоне, ПЕРЕД сохранением.

ФОРМАТ ОТВЕТА ПО КАЖДОМУ ШАГУ:
- Шаг: [Название этапа]
- Почему: [Объяснение архитектурного решения]
- Код: [Полный, рабочий код с подробными комментариями]
- Результат: [Что произойдет после запуска]