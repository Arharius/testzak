Нужно помочь исправить две проблемы в приложении, минимально трогая код.
1. Ошибка при скачивании DOCX (405 Method Not Allowed)
Сейчас при нажатии кнопки “Скачать DOCX” фронтенд получает ошибку 405 Method Not Allowed.
Задача:


Найди в backend/main.py эндпоинт, который отвечает за экспорт/скачивание DOCX (маршрут API и HTTP‑метод).


Найди во фронтенде (скорее всего в backendApi.ts или аналогичном файле) функцию, которую вызывает кнопка “Скачать DOCX”, и проверь, какой URL и HTTP‑метод она использует.


Если путь и/или метод не совпадают, предложи минимальное исправление:


либо изменить декоратор эндпоинта в main.py (@app.get ↔ @app.post или путь),


либо скорректировать метод/URL во фронтовой функции.




Покажи мне точный diff‑патч (конкретные изменения в коде) и коротко объясни, что именно ты меняешь.


Ничего лишнего не рефакторь и не переписывай — цель только убрать 405 и вернуть рабочую кнопку “Скачать DOCX”.


2. Ошибка проверки эквивалентов (401 Authentication Fails, invalid api key)
При нажатии “Проверить эквиваленты” я вижу в UI ошибку вида:
AI error 401: {"error":{"message":"Authentication Fails, Your api key ... is invalid","type":"authentication_error","code":"invalid_request_error"}}.
Задача:


Найди в backend‑коде место, где вызывается внешний AI/эквивалент‑сервис, использующий API‑ключ (скорее всего это функция проверки эквивалентов).


Определи, откуда берётся API‑ключ (конфиг, переменные окружения, константа в коде).


Добавь минимальный безопасный механизм:


проверять наличие ключа и, если он отсутствует/пустой, возвращать со стороны backend понятную ошибку уровня приложения (например, {"ok": false, "error": "AI equivalence check is not configured: missing API key"}) без попытки вызова внешнего сервиса;


не логировать сам ключ и не жёстко хардкодить его в коде.




Если ключ просто устарел, не пытайся его “угадать” — ограничься улучшением обработки ошибки, чтобы фронт не показывал сырое сообщение AI, а получал структурированный ответ.


Покажи diff‑патч изменений и объясни, в каких файлах и строках ты добавил обработку 401.


Общие требования:


Сначала опиши план правок по обоим пунктам.


После моего подтверждения применяй изменения.


Не трогай существующую бизнес‑логику генерации ТЗ, валидации и universality — только экспорт DOCX и обработку 401 у сервиса эквивалентов.