# Генератор ТЗ для госзакупок (44-ФЗ) — документация проекта

> Последнее обновление: 2026-02-25
> Для восстановления работы в новом чате — прочитай этот файл полностью перед любыми правками.

---

## 1. Где что лежит

```
/Users/andres/Downloads/tz_generator/
├── index.html              ← ВЕСЬ КОД (HTML + CSS + JS), ~2700 строк
├── docx.min.js             ← библиотека генерации DOCX (не трогать)
├── html2pdf.bundle.min.js  ← библиотека генерации PDF (не трогать)
├── start.command           ← bash-скрипт: python3 -m http.server 8765
├── CLAUDE.md               ← этот файл
└── index.backup.*.html     ← резервные копии по датам
```

**Запуск локально:** двойной клик на `start.command` → http://localhost:8765/
Или вручную: `cd /Users/andres/Downloads/tz_generator && python3 -m http.server 8765`

---

## 2. Что это за приложение

Одностраничное (SPA) веб-приложение без бэкенда и без базы данных.

**Как работает:**
1. Пользователь выбирает тип товара из каталога (81 тип), вводит модель и API-ключ
2. Приложение отправляет промпт в API ИИ (Groq / OpenRouter / DeepSeek) — напрямую из браузера
3. ИИ возвращает JSON с характеристиками + мета (ОКПД2, КТРУ, статус 175-ФЗ)
4. Приложение формирует ТЗ (разделы 1–5) и предлагает скачать DOCX или PDF

**Где хранятся ТЗ:** нигде — только в браузере до скачивания. Нет БД, нет сервера.

**Каталог товаров:** жёстко в `index.html`, объект `GOODS_CATALOG`.
Чтобы добавить/изменить тип товара — редактируй `index.html`.

---

## 3. Архитектура кода (index.html)

### 3.1 Ключевые глобальные объекты

| Объект | Строки | Назначение |
|---|---|---|
| `GOODS_CATALOG` | ~480–870 | Справочник 81 типа товара. Каждый тип: `name`, `okpd2`, `okpd2name`, `ktruFixed`, `placeholder`, `isSoftware` (для ПО) |
| `GOODS_GROUPS` | ~1180–1200 | Группировка каталога для отображения в выпадающем меню (12 групп) |
| `SW_1236_TYPES` | ~1700 | Массив типов ПО, к которым применяется ПП РФ № 1236 |
| `HW_878_TYPES` | ~1700 | Массив типов железа под ПП РФ № 878 (РЭПР) |
| `HW_175_TYPES` | ~1700 | Массив типов под ПП РФ № 616 |

### 3.2 Ключевые функции JS

| Функция | Назначение |
|---|---|
| `openRowDropdown(id)` | Открывает выпадающий список выбора типа товара. Очищает поле, ставит placeholder = текущий тип |
| `selectRowGoods(rowId, value)` | Выбирает тип товара в строке |
| `handleRowDropdownKey(e, rowId)` | Клавиатурная навигация ↑↓ Enter Escape в дропдауне |
| `buildRowDropdownHTML(rowId, filter, activeVal)` | Строит HTML дропдауна с фильтром. Поиск идёт по: `name`, `okpd2`, `placeholder`, `okpd2name` |
| `buildPrompt(goodsType, model, goodsName)` | Формирует промпт для ИИ. Содержит `promptsByType` — специализированные подсказки по типу товара |
| `generateTZ()` | Главная функция: собирает строки таблицы → вызывает ИИ для каждой → рендерит ТЗ |
| `parseSpecs(text)` | Парсит JSON-ответ ИИ. Поддерживает формат `{meta, specs}` и старый `[...]` |
| `postProcessSpecs(specs, goodsType)` | Автоматически исправляет ответ ИИ для соответствия 44-ФЗ (см. раздел 5) |
| `buildReqTableHTML(goodsType, meta)` | Строит HTML раздела 2 (требования к качеству + НПА-блок нацрежима). Нумерация пунктов — динамическая |
| `buildDeliveryTableHTML(goodsType)` | Строит HTML раздела 4 (гарантия и поставка) |
| `buildOtherTableHTML(goodsType)` | Строит HTML раздела 5 (иные требования) |
| `downloadDOCX()` | Экспорт ТЗ в DOCX. Читает данные из DOM через `buildDocxRowsFromDOM()` |
| `downloadPDF()` | Экспорт ТЗ в PDF через html2pdf |
| `loadDemo()` | Загружает демо-данные (системный блок + монитор) |
| `numToText(n)` | Число → прописью по-русски (для количества в шапке ТЗ) |

### 3.3 Структура раздела 2 (buildReqTableHTML)

Раздел 2 собирается динамически из трёх частей:

```
baseRows (hwRows или swRows)        ← пп. 2.1–2.4 (оборудование) или 2.1–2.3 (ПО)
  +
restrictionRows (НПА-блок)          ← определяется по nacRegime:
  pp878  → пп. 2.5–2.7 (РЭПР + ПП719 + ЕЭК105)
  pp1236 → пп. 2.4–2.5 (запрет иностранного ПО + выписка из реестра Минцифры)
  pp616  → пп. 2.5–2.6 (запрет + СТ-1/ГИСП)
  none   → п. 2.5 (без ограничений)
  +
specialRows (специфика типа)        ← нумерация ДИНАМИЧЕСКАЯ (nextNum = base + restriction + 1)
  Совместимость с ОС, ФСТЭК, ФСБ и т.д.
```

**Важно:** номера в `specialRowsData` не указываются — они вычисляются автоматически в `nextNum`.

---

## 4. Каталог товаров (GOODS_CATALOG) — 81 тип

### Оборудование (железо)
| Ключ | Название | ОКПД2 | Нацрежим |
|---|---|---|---|
| `pc` | Системный блок | 26.20.15.000 | ПП878 |
| `laptop` | Ноутбук | 26.20.11.130 | ПП878 |
| `monoblock` | Моноблок | 26.20.11.130 | ПП878 |
| `server` | Сервер | 26.20.14.000 | ПП878 |
| `tablet` | Планшет | 26.20.11.130 | ПП878 |
| `thinClient` | Тонкий клиент | 26.20.11.130 | ПП878 |
| `monitor` | Монитор | 26.40.20.110 | ПП878 |
| `printer` | Принтер | 28.23.22.110 | ПП878 |
| `mfu` | МФУ | 28.23.22.120 | ПП878 |
| `scanner` | Сканер | 26.20.40.200 | ПП878 |
| `keyboard` | Клавиатура | 26.20.40.120 | ПП878 |
| `mouse` | Мышь | 26.20.40.120 | ПП878 |
| `kvm` | KVM-переключатель | 26.20.40.120 | ПП878 |
| `ups` | ИБП | 27.11.40.000 | ПП878 |
| `projector` | Проектор | 26.70.14.110 | ПП878 |
| `interactive` | Интерактивная доска | 26.20.40.120 | ПП878 |
| `webcam` | Веб-камера | 26.20.16.190 | ПП878 |
| `headset` | Гарнитура | 26.40.43.000 | ПП878 |
| `switch` | Коммутатор | 26.30.11.140 | ПП878 |
| `router` | Маршрутизатор | 26.30.11.150 | ПП878 |
| `firewall` | МЭ (аппаратный) | 26.30.11.140 | ПП878 |
| `accessPoint` | Точка доступа Wi-Fi | 26.30.11.170 | ПП878 |
| `nas` | NAS (сетевое хранилище) | 26.20.14.000 | ПП878 |
| `patchPanel` | Патч-панель | 27.33.19.120 | ПП878 |
| `mediaConverter` | Медиаконвертер | 26.30.11.140 | ПП878 |
| `ssd` | SSD-накопитель | 26.20.40.130 | ПП878 |
| `hdd` | HDD-накопитель | 26.20.22.130 | ПП878 |
| `ram` | Оперативная память | 26.20.40.140 | ПП878 |
| `flashDrive` | Флеш-накопитель | 26.20.40.130 | ПП616 |
| `dvd` | DVD-диски | 26.80.12.000 | ПП616 |
| `tapeLib` | Ленточная библиотека | 26.20.33.000 | ПП878 |
| `patchCord` | Патч-корд | 27.31.20.000 | ПП616 |
| `fiberCable` | Оптический кабель | 27.31.10.000 | ПП616 |
| `hdmiCable` | HDMI-кабель | 27.32.10.190 | ПП616 |
| `powerCable` | Кабель питания | 27.32.10.190 | ПП616 |
| `rackCabinet` | Телекоммуникационный шкаф | 27.33.19.110 | ПП616 |
| `serverRack` | Серверная стойка | 31.09.13.190 | ПП616 |
| `serverBlade` | Блейд-сервер | 26.20.14.000 | ПП878 |
| `san` | СХД (SAN) | 26.20.14.000 | ПП878 |
| `pdu` | PDU (распределитель питания) | 27.33.19.190 | ПП616 |
| `kvm_server` | KVM-сервер (серверный) | 26.30.11.140 | ПП878 |
| `cartridge` | Картридж | 28.23.90.000 | ПП616 |
| `paper` | Бумага офисная | 17.12.14.110 | ПП616 |
| `toner` | Тонер | 20.59.59.000 | ПП616 |
| `drum` | Фотобарабан | 28.23.90.000 | ПП616 |
| `cpu` | Процессор | 26.20.40.160 | ПП878 |
| `gpu` | Видеокарта | 26.20.40.170 | ПП878 |
| `motherboard` | Материнская плата | 26.20.40.150 | ПП878 |
| `psu` | Блок питания | 27.11.40.000 | ПП616 |
| `cooling` | Система охлаждения | 28.29.60.000 | ПП616 |
| `parts` | Комплектующие прочие | 26.20.40.900 | ПП878 |

### Программное обеспечение (ПО) — все под ПП РФ № 1236
| Ключ | Название | Примеры продуктов |
|---|---|---|
| `os` | Операционная система | Astra Linux SE, ALT Linux, РЕД ОС |
| `office` | Офисный пакет | МойОфис, Р7-Офис |
| `virt` | Платформа виртуализации | Базис.vCore, zVirt, Р-Виртуализация |
| `vdi` | VDI / виртуальные рабочие места | **Termidesk**, Базис.WorkPlace, RuDesktop |
| `dbms` | СУБД | PostgreSQL Pro Enterprise, Tantor SE, Jatoba |
| `erp` | ERP / бухгалтерское ПО | 1С:Предприятие, Галактика ERP, Парус |
| `cad` | САПР | КОМПАС-3D, nanoCAD |
| `license` | Лицензия на ПО | (общий тип) |
| `antivirus` | Антивирус | Kaspersky EP, Dr.Web Enterprise |
| `edr` | EDR (защита конечных точек) | Kaspersky EDR, PT Sandbox |
| `firewall_sw` | ПО межсетевого экрана | UserGate, Континент ПО |
| `dlp` | DLP (защита от утечек) | InfoWatch Traffic Monitor, Solar Dozor |
| `siem` | SIEM | MaxPatrol SIEM, RuSIEM |
| `crypto` | СКЗИ | КриптоПро CSP, ViPNet Client |
| `waf` | WAF (защита веб-приложений) | PT Application Firewall, Solar appScreener |
| `pam` | PAM (привилегированный доступ) | Indeed PAM, Senhasegura |
| `iam` | IAM/IdM (управление доступом) | Indeed Access Manager, Solar inRights |
| `pki` | PKI / удостоверяющий центр | КриптоПро УЦ 2.0, ViPNet PKI |
| `email` | Почтовый сервер | **РуПост**, Communigate Pro, Mailion |
| `vks` | ВКС / мессенджер | TrueConf Server, Видеомост, iMind |
| `ecm` | СЭД (документооборот) | ТЕЗИС, Directum RX, DocsVision |
| `portal` | Корпоративный портал | Битрикс24, 1С-Битрикс |
| `project_sw` | Управление проектами | Яндекс.Трекер, Kaiten |
| `bpm` | BPM (бизнес-процессы) | ELMA BPM, Comindware |
| `backup_sw` | Резервное копирование | **RuBackup**, Кибер Бэкап, Bacula |
| `itsm` | ITSM | SimpleOne, Naumen Service Desk |
| `monitoring` | Мониторинг ИТ | Zabbix, MaxPatrol VM |
| `mdm` | MDM (мобильные устройства) | SafePhone, МобилДок |
| `hr` | HRM (управление персоналом) | 1С:ЗУП, Добыто HRM |
| `gis` | ГИС | NextGIS, Панорама ГИС |

### Группы в меню (GOODS_GROUPS)
1. 💻 Компьютеры: pc, laptop, monoblock, server, tablet, thinClient
2. 🖨️ Периферия и оргтехника: monitor, printer, mfu, scanner, keyboard, mouse, kvm, ups, projector, interactive, webcam, headset
3. 🌐 Сетевое оборудование: switch, router, firewall, accessPoint, nas, patchPanel, mediaConverter
4. 💾 Накопители и носители: ssd, hdd, ram, flashDrive, dvd, tapeLib
5. 🔌 Кабели и коммутация: patchCord, fiberCable, hdmiCable, powerCable, rackCabinet
6. 🖥️ Серверное оборудование: serverRack, serverBlade, san, pdu, kvm_server
7. 🖨️ Расходные материалы: cartridge, paper, toner, drum
8. 💿 Системное и офисное ПО: os, office, virt, vdi, dbms, erp, cad, license
9. 🔒 Средства защиты информации: antivirus, edr, firewall_sw, dlp, siem, crypto, waf, pam, iam, pki
10. 📧 Коммуникации и совместная работа: email, vks, ecm, portal, project_sw, bpm
11. 🛠️ Управление ИТ и инфраструктурой: backup_sw, itsm, monitoring, mdm, hr, gis
12. 📦 Комплектующие: cpu, gpu, motherboard, psu, cooling, parts

---

## 5. Нормативная база (реализована в коде)

> Важно: с 01.01.2025 базовые меры национального режима в закупках применяются по ПП РФ от 23.12.2024 № 1875.  
> Внутренние теги `pp1236` / `pp878` / `pp616` в коде сохранены как **классификаторы типов товара**, а не как самостоятельные актуальные основания запрета/ограничения.

### НПА-блоки в разделе 2 (buildReqTableHTML → restrictionRows)

| nacRegime | НПА | Пункты раздела 2 | Суть |
|---|---|---|---|
| `pp878` | **ПП РФ № 1875** + реестры промтоваров (ГИСП/ЕАЭС), Решение Совета ЕЭК № 105 | 2.5 нацрежим, 2.6 подтверждение происхождения, 2.7 ПП719 (при применимости) | Ветка для радиоэлектроники: в тексте указывается применение защитных мер по актуальной редакции ПП1875, подтверждение происхождения через предусмотренные реестры/документы |
| `pp1236` | **ПП РФ № 1875** + ПП РФ № 1236 (правила ведения реестра ПО) | 2.4 нацрежим, 2.5 подтверждение реестровой записи ПО | Ветка для ПО: меры нацрежима по ПП1875; подтверждение российского/евразийского ПО через реестры (Минцифры/ЕАЭС) |
| `pp616` | **ПП РФ № 1875** + ПП РФ № 719 (при применимости) | 2.5 нацрежим, 2.6 подтверждение происхождения | Ветка для прочих промтоваров: применение мер по ПП1875, подтверждение происхождения документами и реестровыми записями по действующей редакции НПА |
| `none` | — | 2.5 без ограничений | Страна происхождения указывается в ТТД, ограничений нет |

### Важные правила формулировок (исправлены)
- ПП1875: в тексте раздела 2 используется как **основное действующее основание** нацрежима (44-ФЗ и 223-ФЗ)
- Для ПО: указывается связь с реестром Минцифры и ПП1236 как НПА по ведению реестра
- Для промтоваров: подтверждение происхождения через ГИСП/ЕАЭС по применимым НПА (ПП719, акты ЕАЭС)
- п. 2.6 оборудование: **«выписка из ГИСП»** (не «заключение Минпромторга» — такого документа не существует)
- УПД ≠ накладная: п. 4.8 — «УПД или товарная накладная (ТОРГ-12)» (разные документы)
- ОС-совместимость: **«или эквивалентными»** обязательно (ч. 3 ст. 33 44-ФЗ)
- ФСТЭК МЭ: два отдельных пункта — МЭ и СОВ (разные профили защиты)
- ФСБ СКЗИ: **«не ниже класса КС1»** (не «КС1 и/или КС2» — некорректная формулировка), ссылка на Приказ ФСБ № 66

### postProcessSpecs — автоматические исправления ответа ИИ

| Что проверяется | Что делается |
|---|---|
| Бренды (Intel, AMD, Samsung, Seagate...) без «или эквивалент» | Добавляет «или эквивалент» |
| Вес/масса с `>=` | Меняет на `<=` ✅ |
| Аккумулятор в `Вт` | Меняет на `Вт·ч` ✅ |
| Единицы GHz/MHz/GB/MB | → ГГц/МГц/ГБ/МБ ✅ |
| Сокет процессора (LGA, AM5, TR4...) | Предупреждение ⚠️ (нарушает ст. 33 44-ФЗ) |
| DDR4 без «или выше» | → «DDR4 или выше» ✅ |
| IPS без «или эквивалент» | → «IPS или эквивалент (угол обзора не менее 178°)» ✅ |
| Точное разрешение 1920x1080 | → «не менее 1920x1080» ✅ |
| Время работы аккумулятора без методики | Добавляет «в режиме офисной работы...» ✅ |

---

## 6. UX — выпадающий список товаров (комбо-дропдаун)

- **Открытие:** клик на поле или стрелку → поле очищается, текущий товар уходит в `placeholder`
- **Поиск:** вводить прямо — фильтрует по `name`, `okpd2`, `okpd2name`, **`placeholder`** (важно! — по placeholder ищутся торговые марки: Termidesk → vdi, RuBackup → backup_sw)
- **Клавиатура:** ↑↓ навигация, Enter — выбор, Escape — закрыть
- **Выбор:** клик или Enter → поле заполняется названием, дропдаун закрывается, фокус переходит на «Модель/описание»

---

## 7. Экспорт DOCX / PDF

### DOCX (downloadDOCX)
- Использует `docx.min.js` (docx.js) — локальная библиотека
- Данные берутся **из DOM** через `buildDocxRowsFromDOM('#tz-req-table')` и т.д.
- Структура: шапка → раздел 1 (сводная таблица) → раздел 2 (требования) → раздел 3 (характеристики по каждой позиции) → раздел 4 (поставка) → раздел 5 (иные) → подписи

### PDF (downloadPDF)
- Использует `html2pdf.bundle.min.js`
- Рендерит DOM-элемент `#tz-output` в PDF

---

## 8. API-провайдеры (поддерживаются)

| Провайдер | Базовый URL | Рекомендуемые модели |
|---|---|---|
| Groq | https://api.groq.com/openai/v1 | llama-3.3-70b-versatile, mixtral-8x7b |
| OpenRouter | https://openrouter.ai/api/v1 | google/gemini-flash-1.5, mistralai/mixtral-8x7b |
| DeepSeek | https://api.deepseek.com | deepseek-chat |

Запросы идут прямо из браузера (fetch) — CORS разрешён у всех провайдеров. API-ключ хранится только в localStorage.

---

## 9. История изменений (всё что сделано с нуля)

### Сессия 1 (начальная разработка)
- Создан `index.html` с нуля: форма, каталог ~40 типов, AI-генерация, DOCX-экспорт
- Реализован `postProcessSpecs` с базовыми исправлениями
- Добавлен PDF-экспорт

### Сессия 2
- Исправлен дублирующийся click-обработчик (`document.addEventListener('click', ...)`)
- Улучшен комбо-дропдаун: клавиатурная навигация (↑↓/Enter/Escape), класс `.kb-focus`
- Заменён `numToText` — полный алгоритм числа прописью (тысячи, миллионы, правильный родительный падеж)
- Удалён мёртвый код: `buildDropdownHTML`, `openGoodsDropdown`, `filterGoods`, `selectGoods`, `onGoodsTypeChange`
- Исправлены ОКПД2: webcam `26.70.13.110` → `26.20.16.190`, NAS/SAN → `26.20.14.000-00000189`
- Исправлены демо-данные (убран сокет LGA 1700)
- **Исправлен поиск**: `openRowDropdown` теперь очищает поле, текущий товар → в placeholder

### Сессия 3 (юридический аудит + расширение каталога)
- **Аудит НПА** — исправлены все формулировки нацрежима:
  - ПП878: правильный механизм «третий лишний» (отклонение при 2+ заявках), убрана ссылка на 488-ФЗ
  - п.2.6: «выписка из ГИСП» вместо несуществующего «заключения Минпромторга»
  - ПП616: «запрет» вместо «ограничения»
  - ПП1236: полное название реестра + ссылка на ст. 12.1 149-ФЗ
  - п.4.8: «УПД или ТОРГ-12» (разделены два разных документа)
- **ОС-совместимость**: добавлено «или эквивалентными» + полное название реестра Минцифры + ссылка на ч. 3 ст. 33 44-ФЗ
- **ФСТЭК МЭ**: разбито на два пункта (МЭ и СОВ — разные профили), убрано «и/или»
- **ФСБ СКЗИ**: «не ниже класса КС1» + ссылка на Приказ ФСБ № 66
- **Нумерация раздела 2**: стала динамической (`nextNum = base + restriction + 1`) — номера specialRows больше не захардкожены
- **Поиск в каталоге**: добавлен поиск по `placeholder` и `okpd2name` → торговые марки (Termidesk, RuBackup, TrueConf...) находятся поиском
- **Расширен каталог с ~55 до 81 типа** — добавлены:
  - VDI (Termidesk, Базис.WorkPlace, RuDesktop)
  - Почта (РуПост, Mailion, Communigate Pro)
  - ВКС (TrueConf, iMind, МТС Линк)
  - СЭД (ТЕЗИС, Directum RX, DocsVision)
  - Корпоративный портал, BPM, Управление проектами
  - EDR, WAF, PAM, IAM/IdM, PKI/УЦ
  - ITSM (SimpleOne, Naumen), Мониторинг (Zabbix), MDM (SafePhone), HRM (1С:ЗУП), ГИС
  - Резервное копирование обновлён (RuBackup, Кибер Бэкап, Bacula)
- **Реорганизованы группы каталога**: 8 → 12 групп, выделены «🔒 Средства защиты», «📧 Коммуникации», «🛠️ Управление ИТ»
- Для каждого нового типа: AI-промпт с правильными параметрами + specialRows (требования совместимости с отечественными ОС, ФСТЭК/ФСБ где нужно)

### Сессия 4 (улучшение автоопределения типа по модели)
- Расширен словарь `MODEL_TO_TYPE` для оборудования:
  - добавлены линейки ноутбуков (`Vivobook`, `Zenbook`, `ThinkPad`, `MacBook`, `MateBook`, `ProBook`, `EliteBook`, `Inspiron`, `Latitude` и др.) → тип `laptop`
  - добавлены оптические носители (`DVD`, `DVD-R/RW`, `CD-R/RW`, `Blu-ray`, `BD-R/RE`) → тип `dvd`
- В `autoDetectGoodsType` добавлена нормализация ввода (`normalizeDetectText`):
  - унификация регистра, `ё/е`, спецсимволов и пробелов
  - устойчивое частичное совпадение по нормализованным ключам
- Результат: вводы вида `Asus Vivobook ...` и `DVD-R ...` автоматически подставляют корректный тип товара

### Сессия 5 (фиксы автодетекта + актуализация нацрежима 2025+)
- Исправлен ложный автодетект при посимвольном вводе:
  - устранён источник ошибки `key.includes(val)` для коротких префиксов (`"гр"` больше не уводит в `dbms`)
  - частичное совпадение запускается только при длине ввода от 4 символов (точные алиасы работают всегда)
  - для коротких алиасов (`p7`, `1c`, `dvd`) добавлена проверка по границам слова
- Добавлены алиасы для российских брендов:
  - `Гравитон/Graviton` (в т.ч. `Graviton N*`) — корректное определение как оборудование (`pc`/`laptop`)
  - `Aquarius`, `iRU`, `Yadro` — базовое определение как оборудование
- Актуализированы правовые формулировки раздела 2 и предупреждающих блоков:
  - основной НПА для мер нацрежима: **ПП РФ № 1875 от 23.12.2024**
  - ссылки на ПП1236/ПП719 и реестры сохранены как подтверждающие/реестровые механики
  - добавлена явная подсказка пользователю проверять актуальную редакцию перечней ПП1875 перед публикацией в ЕИС
- Добавлен регрессионный скрипт глубоких проверок: `tests/deep_checks.cjs`
  - проверяет автодетект (включая кейсы `Asus Vivobook`, `DVD-R`, `Гравитон`)
  - проверяет согласованность `GOODS_CATALOG` с массивами `SW_1236_TYPES/HW_878_TYPES/HW_175_TYPES`
  - проверяет наличие ключевых правовых формулировок и отсутствие запрещённых фраз
- Добавлен расширенный матричный тест автодетекта: `tests/autodetect_matrix.cjs`
  - проверяет 180 брендовых/продуктовых кейсов по типам товаров
  - формирует отчёты `tests/autodetect_report.md` и `tests/autodetect_report.json`
  - по итогам теста добавлены дополнительные синонимы `Кибер Бэкап / Кибербэкап`
- Добавлен `Makefile` для единого запуска регрессий:
  - `make test` — запускает `tests/autodetect_matrix.cjs` и `tests/deep_checks.cjs`

### Сессия 6 (реальный browser e2e smoke)
- Добавлен полноценный браузерный e2e-скрипт: `tests/e2e_browser_check.cjs`
  - проверяет автодетект (`Гравитон`, `Asus Vivobook`, `DVD-R`)
  - проверяет рендер демо-данных
  - проверяет экспорт `DOCX` и `PDF` (скачивание файлов)
  - проверяет генерацию ТЗ в 44-ФЗ и 223-ФЗ режимах с mock-ответом API
- Добавлен запуск e2e одной командой:
  - `tests/run_e2e.sh`
  - `make e2e`
- Артефакты e2e сохраняются в папку: `tests/e2e_artifacts/`

### Сессия 7 (автодеплой на PythonAnywhere)
- Добавлен скрипт деплоя в PythonAnywhere через официальный API:
  - `scripts/deploy_pythonanywhere.sh`
  - Создаёт webapp (если нет), загружает `index.html`, `docx.min.js`, `html2pdf.bundle.min.js`,
    настраивает static mapping `/` → директория сайта, выполняет reload webapp
- Добавлен make-таргет:
  - `make deploy-pythonanywhere`
  - требуется задать env-переменные: `PA_USERNAME`, `PA_API_TOKEN` (опционально `PA_HOST`, `PA_DOMAIN`, `PA_PYTHON_VERSION`, `PA_SITE_PATH`)

### Сессия 9 (полноценный DOCX-экспорт React-версии + перенос логики из legacy)
- Проведён полный аудит: React-версия (`/react/`) генерировала plain-text вместо таблиц → DOCX не соответствовал образцу 44-ФЗ
- Созданы новые модули React-фронтенда:
  - `frontend-react/src/data/goods-catalog.ts` — полный каталог 91 типа товара (81 основных + инструменты/хоз) с ОКПД2, КТРУ, группами, нацрежимом; функция `detectGoodsType` с 30+ токен-правилами
  - `frontend-react/src/utils/spec-processor.ts` — перенесён `postProcessSpecs` (все 10 правил: марки «или эквивалент», сокеты, вес, батарея, единицы, DDR, матрица, разрешение, время работы, мат. операторы); `parseAiResponse` с поддержкой `{meta, specs}` и strip markdown
  - `frontend-react/src/utils/npa-blocks.ts` — перенесены `buildSection2Rows` / `buildSection4Rows` / `buildSection5Rows` с полным НПА-блоком (pp878/pp1236/pp616/none для 44-ФЗ и 223-ФЗ), актуальные формулировки ПП РФ № 1875, специальные требования по 30+ типам товара
- Полностью переписан `frontend-react/src/components/Workspace.tsx`:
  - Каталог: 91 тип в группированном `<select>` с `<optgroup>` по 12 группам (вместо 11 типов)
  - `exportDocx()` → полноценная функция `buildDocx()` на docx.js v9:
    - Раздел 1: таблица 6 строк × 2 колонки (Наименование, ОКПД2, КТРУ, ОКЕИ, Количество, Дата) для одной позиции; сводная 5-колоночная для нескольких
    - Раздел 2: 2-колоночная таблица НПА-требований с окраской ⚖️-строк в жёлтый
    - Раздел 3: 4-колоночная таблица характеристик (№, Наименование, Значение, Ед.изм.) с групповыми заголовками через `columnSpan: 4` и синей шапкой
    - Раздел 4: 2-колоночная таблица гарантии/поставки
    - Раздел 5: 2-колоночная таблица иных требований
    - Подписи: таблица Заказчик + Дата
  - `exportPdf()` — текстовый PDF со всеми 5 разделами
  - Предварительный просмотр в браузере: полный рендер таблиц 1–5 прямо на странице (раньше был только JSON в textarea)
  - Статус строки: показывает кол-во характеристик (`✅ Готово (27 хар-к)`)
  - Кнопки DOCX/PDF активируются только после успешной генерации
- Сборка `npm run build` прошла без ошибок TypeScript (514 модулей)
- Устранены юридические ошибки образца:
  - «заключение Министерства промышленности» → «выписка из ГИСП»
  - «ПП РФ № 878» → актуальная ссылка «ПП РФ № 1875 от 23.12.2024»
  - «товарная накладная (УПД)» → «УПД или товарная накладная (ТОРГ-12)»
  - В образце был сокет LGA 1700 → теперь `postProcessSpecs` добавляет предупреждение ⚠️

### Сессия 8 (расширенный автодетект ИТ-терминов + fallback через ИИ)
- Существенно расширен словарь `MODEL_TO_TYPE`:
  - добавлены массовые ИТ-алиасы для кабелей и коммутации (`utp`, `stp`, `s/ftp`, `cat5e/cat6/cat6a`, `rj45`, `ethernet`, `оптика`, `om3/om4`, `hdmi/displayport`, `power cord`)
  - добавлены дополнительные ключевые термины по серверному/сетевому/периферийному оборудованию и комплектующим
- Исправлен кейс пользователя: ввод `utp` теперь определяет тип `patchCord` (кабель витая пара)
- Добавлен fallback-автодетект через ИИ для неизвестных терминов:
  - если локальный словарь не дал результат, при наличии API-ключа выполняется debounce-запрос к выбранному провайдеру (`DeepSeek/OpenRouter/Groq`)
  - есть кэш результатов и защита от частых запросов при наборе текста
  - в UI показывается метка `🤖 Тип определён через ИИ...`
- Регрессии пройдены:
  - `make test` => `autodetect_matrix 180/180`, `deep_checks passed`

---

## 10. Как обновить на Netlify (деплой)

Приложение статическое — деплой = загрузка одного файла `index.html`.

### Способ 1: Netlify Drop (без регистрации CLI)
1. Открыть https://app.netlify.com/drop
2. Перетащить папку `/Users/andres/Downloads/tz_generator/` (всю папку, не файл)
3. Получить URL вида `https://xxx.netlify.app`

### Способ 2: Netlify CLI
```bash
# Установить (если нет)
npm install -g netlify-cli

# Деплой с токеном
NETLIFY_AUTH_TOKEN=<ваш_токен> netlify deploy \
  --dir /Users/andres/Downloads/tz_generator \
  --prod \
  --message "update $(date +%Y-%m-%d)"
```

### Обновление (после правки index.html)
Повторить любой из способов — Netlify автоматически заменит старую версию.

---

## 11. Как сделать резервную копию вручную

```bash
cp /Users/andres/Downloads/tz_generator/index.html \
   /Users/andres/Downloads/tz_generator/index.backup.$(date +%Y%m%d_%H%M%S).html
```

Резервные копии хранятся в той же папке с датой в имени файла.

---

## 12. Типичные задачи для нового чата

### Добавить новый тип товара
1. В `GOODS_CATALOG` добавить объект: `{ name, okpd2, okpd2name, ktruFixed, placeholder, isSoftware? }`
2. В `GOODS_GROUPS` добавить ключ в нужную группу
3. В `promptsByType` (внутри `buildPrompt`) добавить шаблон характеристик
4. В `specialRowsData` (внутри `buildReqTableHTML`) добавить специфические требования
5. Если ПО — добавить ключ в массив `SW_1236_TYPES`; если железо с ПП878 — в `HW_878_TYPES`

### Изменить формулировку НПА
- Раздел 2 → функция `buildReqTableHTML` → блоки `if (nacRegime === 'pp878')` и т.д.

### Изменить требования к поставке (раздел 4)
- Функция `buildDeliveryTableHTML`

### Изменить иные требования (раздел 5)
- Функция `buildOtherTableHTML`

### Добавить нового AI-провайдера
- Функция `callAI` (или аналог) — добавить в switch/if по выбранному провайдеру

---

*Файл создан 2026-02-20. При каждом значимом изменении — обновлять раздел 9 «История изменений».*
