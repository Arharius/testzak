Ты — Senior QA Automation Engineer. Твоя предыдущая реализация не подходит: ты проверил только один файл и сделал заглушку (mock) вместо реального тестирования ввода данных. Качество важнее скорости! Мне нужен 100% рабочий, боевой код.

Я загрузил несколько файлов шаблонов .docx (например, TZ_2026-03-20, TZ_2026-03-21 и т.д.) в корневую папку проекта. 

ЗАДАЧА: Написать полный, готовый к запуску скрипт `run_all_tests.py`, который проведет глубокий E2E-тест для КАЖДОГО файла .docx в проекте.

ТРЕБОВАНИЯ К КОДУ (ЗАПРЕЩЕНО использовать TODO, pass и заглушки):

1. Поиск всех файлов:
- Используй модуль `glob` или `os`, чтобы найти ВСЕ файлы с расширением `.docx` в корневой папке (кроме тех, что уже лежат в папке test_output).
- Скрипт должен в цикле обрабатывать каждый найденный файл.

2. Симуляция пользовательского ввода (Генерация):
- Для каждого файла скрипт должен открыть его с помощью библиотеки `python-docx` (Document).
- Скрипт должен сымитировать ввод пользователя: найти в таблицах и абзацах текст "комплект товаров (8 позиций)" (или аналогичный плейсхолдер наименования объекта закупки) и РЕАЛЬНО ЗАМЕНИТЬ его на строку "ноутбук asus x1503".
- Сохранить измененный файл в папку `test_output/` с префиксом `tested_` (например, `test_output/tested_TZ_2026-03-21.docx`). Если папки нет — создать её.

3. Глубокий парсинг и валидация (Asserts):
- После сохранения скрипт должен заново открыть файл из папки `test_output/`, считать ВЕСЬ текст (из всех абзацев и всех ячеек таблиц) в одну большую строковую переменную.
- Выполнить проверки (assert или if-raise):
  а) Позитивный тест: Текст "ноутбук asus x1503" ДОЛЖЕН присутствовать.
  б) Позитивный тест: ДОЛЖНЫ присутствовать фразы: "Цена Договора является твёрдой" и "лицензию ФСТЭК России".
  в) Негативный антимонопольный тест (КРИТИЧНО): В тексте НЕ ДОЛЖНО БЫТЬ слов "выписку" (в контексте Минцифры), аббревиатуры "НДВ", и фразы "не ранее чем за 12 месяцев". Если они есть — скрипт должен вывести в консоль ошибку с указанием имени файла и проблемной фразы, но продолжить проверку остальных файлов.

Формат ответа:
Напиши ПОЛНЫЙ код файла `run_all_tests.py` с подробными комментариями к каждой функции. Код должен быть готов к копированию и запуску.